{
  "name": "heddle",
  "version": "0.1.0",
  "description": "Deterministic multi-agent orchestration: plain JS scripts fan out LLM subagents; control flow is code, only the leaves are model-driven.",
  "type": "module",
  "main": "src/index.js",
  "exports": {
    ".": "./src/index.js",
    "./backends/anthropic": "./src/backends/anthropic.js"
  },
  "scripts": {
    "test": "vitest run",
    "lint": "node --check src/*.js src/backends/*.js examples/*.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "ajv": "8.20.0"
  },
  "devDependencies": {
    "vitest": "4.1.11"
  }
}
